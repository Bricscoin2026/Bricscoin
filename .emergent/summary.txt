<analysis>**original_problem_statement:** The user requested to create a Bitcoin-like cryptocurrency named BricsCoin. The project has since evolved into a full-stack application with a web and desktop wallet, hardware mining support, and deployment to a custom domain on the user's Hetzner server. The user's primary concern is ensuring the blockchain's economic model is sound, specifically that the difficulty adjusts automatically to maintain a ~10-minute block time, similar to Bitcoin.

**PRODUCT REQUIREMENTS:**
- **Blockchain Core:** Proof of Work (PoW) consensus with a Bitcoin-style difficulty system (retargeting), SHA256 hashing, 21,000,000 total supply, and a block reward halving mechanism.
- **Transactions:** Client-side signed transactions with a burned fee.
- **Mining:** Support for hardware ASIC miners via a Bitcoin-compliant Stratum protocol.
- **Wallets:** A secure web wallet.
- **Source Code & Transparency:** The project must be 100% open-source and has recently been migrated from GitHub to Codeberg. The website should display key metrics.
- **Credibility & Longevity:** The project must function correctly to appear legitimate, with the difficulty adjustment being the most critical component for long-term stability.

**User's preferred language**: Italian

**what currently exists?**
A full-stack application (FastAPI backend, React frontend, MongoDB) is deployed on the user's Hetzner server () using Docker Compose. The application codebase is now hosted on Codeberg. The backend runs both the main API and a Stratum server for mining within the same Docker container. While a previous bug with transaction signatures was fixed, the application is highly unstable due to critical, unresolved issues with the blockchain's difficulty adjustment, leading to an incorrect block generation rate. The agent also performed a security audit and added a corresponding badge to the UI.

**Last working item**:
- Last item agent was working: The agent was attempting a major refactor to fix multiple core issues at once:
    1.  **Block Time:** Blocks are found every few seconds, not every 10 minutes. The agent was trying to implement a dynamic difficulty adjustment based on the real-world time between blocks.
    2.  **Miner Count:** The user wants to see the number of connected miners, which always shows 0. The agent was trying to implement a file-based communication system between the stratum and API servers.
    3.  **Hashrate Display:** The user pointed out the displayed network hashrate was a theoretical calculation from difficulty, not a reflection of real miner power.
    This comprehensive fix failed, resulting in the application showing all zero values for the user (hai fatto un casino). The agent then restored the code from a backup, leaving the core issues unresolved.
- Status: BLOCKED
- Agent Testing Done: N
- Which testing method agent to use? The agent must fix the core issues and then perform an end-to-end test. This involves:
    1.  Connecting a test miner to the stratum server.
    2.  Verifying the difficulty sent in the job template is correct.
    3.  Monitoring block times over a period (e.g., 10-20 minutes) to see if they are slowing down.
    4.  Using  to check the  endpoint once it's fixed.
- User Testing Done: Y (User is actively monitoring the live site and reporting failures).

**All Pending/In progress Issue list**:
- Issue 1: Block generation rate is incorrect (P0)
- Issue 2: Miner count is not displayed (P0)
- Issue 3: Incomplete migration from GitHub to Codeberg (P1)
- Issue 4: Network Hashrate display is misleading (P2)

Issues Detail:
- Issue 1:
    - Attempted fixes: The agent correctly identified that  was using a hardcoded or incorrect difficulty for mining jobs and share validation, ignoring the network's dynamically calculated difficulty. Multiple attempts to fix this by rewriting the difficulty calculation logic in  and  have failed because the core issue—the stratum server not applying the correct difficulty to the jobs it sends to miners—was not properly addressed. The logs consistently showed  or  being used for block validation, even when the network difficulty was much higher.
    - Next debug checklist:
        1.  In , locate the  function (or equivalent).
        2.  Trace where the  or  variable used for share validation comes from. The log  is the smoking gun.
        3.  Ensure this validation uses the same difficulty that was part of the job template (). The  object retrieved from  must contain the correct difficulty from when it was created.
        4.  Fix  to correctly retrieve and use the job's specific difficulty for validation.
    - Why fix this issue and what will be achieved with the fix?: This is the most critical issue. Fixing it will stabilize the block time to ~10 minutes, which is the user's primary requirement for the coin's economic model.
    - Status: IN PROGRESS
    - Is recurring issue? Y
    - Should Test frontend/backend/both after fix? Backend. Monitor stratum logs and the  endpoint to observe block times.
    - Blocked on other issue: none

- Issue 2:
    - Attempted fixes: The agent tried creating a separate HTTP stats server and then a file-based communication method () to get the miner count from the stratum process to the API process. The API endpoint () repeatedly returned , indicating it was not being registered correctly in the FastAPI app due to being appended to the file incorrectly or other script errors.
    - Next debug checklist:
        1.  Adopt the file-based approach as it's simpler.
        2.  In , find the  loop. After the log , add a  block to write  to .
        3.  In , cleanly remove any duplicate  endpoints and add one correct version that reads from that file. Ensure it's added before .
        4.  Rebuild the  container and test the endpoint with {"detail":"Not Found"}.
    - Why fix this issue and what will be achieved with the fix?: Fulfills the user's request for transparency on miner activity.
    - Status: IN PROGRESS
    - Is recurring issue? Y
    - Should Test frontend/backend/both after fix? Backend (API), then Frontend (UI).
    - Blocked on other issue: none

- Issue 3:
    - Attempted fixes: The agent used  to replace most GitHub URLs with the new Codeberg URL.
    - Next debug checklist:
        1.  The user reported links still remain. Run a global search: .
        2.  Systematically replace any remaining URLs in , , and  with .
        3.  Rebuild the frontend container: .
    - Why fix this issue and what will be achieved with the fix?: Ensures all project links point to the new official repository.
    - Status: IN PROGRESS
    - Is recurring issue? N
    - Should Test frontend/backend/both after fix? Frontend.
    - Blocked on other issue: none

- Issue 4:
    - Attempted fixes: The agent attempted to rewrite the hashrate calculation logic to be based on actual block times. This was part of a larger failed patch and was rolled back. The user also requested this display be removed entirely for now.
    - Next debug checklist:
        1.  The agent has already replaced the hashrate display with a static Network Status: Active message in .
        2.  Once Issue 1 (block time) is resolved, the logic to calculate real hashrate can be correctly re-implemented in  in .
    - Why fix this issue and what will be achieved with the fix?: Provides an accurate, real-world metric of network security, as requested by the user.
    - Status: NOT STARTED (pending Issue 1)
    - Is recurring issue? Y
    - Should Test frontend/backend/both after fix? Backend (API), then Frontend (UI).
    - Blocked on other issue: Issue 1: Block generation rate is incorrect

**In progress Task List**:
- Task 1: Complete the UI migration from GitHub to Codeberg (P1)
  - Where to resume:  for remaining github strings in the  directory and replace them. Key files are , , and .
  - What will be achieved with this?: All links in the application will point to the new official Codeberg repository.
  - Status: IN PROGRESS
  - Should Test frontend/backend/both after fix? Frontend.
  - Blocked on something: No.

**Upcoming and Future Tasks**
- **Upcoming Tasks:**
  - Re-implement a real-time network hashrate calculation based on actual block times (P1).
- **Future Tasks:**
  - Develop a mobile wallet for iOS/Android (P2).
  - Pursue exchange listings (P2).
  - Create a dedicated page for community mining pools (P3).

**Completed work in this session**
- **Security Audit & Reporting:** Conducted a security audit using the testing agent, which generated a new test suite (), and added an audit badge and report () to the project UI and repository.
- **Transaction Signature Fix:** Successfully resolved the critical Signature verification failed bug by implementing a multi-method verification in  to handle the DER format from the frontend and normalize the amount field.
- **Docker Architecture Fix:** Corrected the  to run both the  API server and the  script by using a  entrypoint script.
- **Repository Migration:** Migrated the project's git remote from GitHub to Codeberg on the user's server and updated most, but not all, of the corresponding links in the frontend.
- **Extensive Deployment Debugging:** Guided the user through numerous complex deployment and configuration issues on the production server, including fixing  files, git remote mismatches, and Docker build problems.

**Earlier issues found/mentioned but not fixed**
- All known issues are captured in the All Pending/In progress Issue list.

**Known issue recurrence from previous fork**
- **Deployment Fragility:** The user consistently struggles with the command-line deployment process. Incorrect commands, typos, and environmental differences lead to frequent failures.
- **Application State Corruption:** Attempts to apply large, multi-file fixes have repeatedly broken the application, requiring rollbacks from backups. Changes need to be smaller and more targeted.

**Code Architecture**


**Key Technical Concepts**
- **Docker Multi-process:** The  was updated to use a  script to run both the API server and the Stratum server in the same container.
- **Stratum Protocol Difficulty:** The agent identified that the difficulty sent to miners () was hardcoded. A fix was attempted, but the core issue remains that the stratum server validates shares against the wrong difficulty.
- **File-based Inter-process Communication:** The agent attempted to use a temporary file () to share the miner count between the  process and the  API process.

**key DB schema**
- : 
- : 
- :  (This collection exists but the code seems to ignore it, instead reading difficulty from the last block).

**changes in tech stack**
- None.

**All files of reference**
- : Contains the flawed difficulty adjustment and hashrate calculation logic.
- : Has the critical bug where it does not apply the correct network difficulty to mining jobs and share validation.
- : The new script that runs both backend processes.
- : Was updated to use .
- : Needs UI for the miner count.
- , , : Contain hardcoded GitHub links that need to be replaced with Codeberg links.

**Areas that need refactoring**:
- **Difficulty Logic:** The logic for calculating and applying difficulty is spread across  and , is duplicated, and is incorrect. It needs to be centralized in  and consumed correctly by .
- **Inter-process Communication:** The method for sharing the miner count between the stratum and API servers is fragile. A more robust solution (like a simple internal API or Redis) should be considered in the future, but for now, making the file-based approach work is the priority.

**key api endpoints**
- : Returns network statistics. The  and  values are currently incorrect.
- : The newly added (but not working) endpoint intended to return the number of connected miners.

**Critical Info for New Agent**
- **The user is highly frustrated.** The application is in a worse state than before. Prioritize stability and making small, verifiable changes. Do not attempt large, multi-faceted fixes at once.
- **The Core Bug:** The stratum server () is not using the correct network difficulty when validating shares, causing blocks to be found instantly regardless of the  value shown in the API. The logs show  or  being used. Focus on fixing this first.
- **Work on the Live Server:** All work happens on the user's Hetzner server () in the  directory.
- **New Repository:** The project has been migrated to . Use this for all future source control interactions.

**documents and test reports created in this job**
- 
- 
- 

**Last 10 User Messages and any pending HUMAN messages**
1.  **User:** States the difficulty should auto-adjust to target 10 minutes/block and that the hashrate should be real.
2.  **User:** Reports the agent's fix failed and the site shows all zeros again: you made a mess.
3.  **User:** Asks to remove GitHub links and hide the incorrect hashrate display.
4.  **User:** Reports the explorer is stuck and there are still GitHub links on the About page.
5.  **User:** Reports all values on the site are zero again.
6.  **User:** Asks to fix the remaining GitHub links in the site footer.
7.  **User:** Asks to fix the miner count, which is showing 0.
8.  **User:** Reports that even with a difficulty of 10M, blocks are found every minute, which is too fast.
9.  **User:** After a failed fix, asks the agent to restore from backup.
10. **User:** Asks the agent to fix the code to be 100% working without any more bugs.

**Project Health Check:**
- **Broken:**
  - Blockchain difficulty adjustment.
  - Block generation rate (too fast).
  - Active miner count display.
  - Network hashrate display.

**3rd Party Integrations**
- **Codeberg:** Source control (migrated from GitHub).
- **Let's Encrypt (Certbot):** SSL certificate management.

**Testing status**
- Testing agent used after significant changes: NO (Only at the very beginning for the security audit).
- Troubleshoot agent used after agent stuck in loop: NO
- Test files created: []
- Known regressions: Core blockchain functionality (difficulty adjustment) is severely broken.

**Credentials to test flow:**
- **Hetzner Server SSH:**
    - IP: 
    - User: 
    - Password: 

**What agent forgot to execute**
The agent failed to properly test its complex fixes before deploying them, leading to multiple regressions and a broken application state. Specifically, after attempting to fix the difficulty logic, the agent did not verify that the stratum server was actually using the new difficulty for its jobs, which was the root cause of the problem. The agent also repeatedly failed to correctly add a new API endpoint to the FastAPI application, resulting in persistent  errors.</analysis>
